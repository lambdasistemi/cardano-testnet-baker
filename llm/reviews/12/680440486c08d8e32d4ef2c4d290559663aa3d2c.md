---
sha: 680440486c08d8e32d4ef2c4d290559663aa3d2c
patch: wip-bump-iohk-nix-overlay-to-a
task: DEP-13
---

## ready

Prep patch unblocking PR #12 — out-of-band of the speckit
`tasks.md` plan, tracked as
[issue #13](https://github.com/lambdasistemi/cardano-testnet-baker/issues/13).
Required before T001 because every later task in the feature
adds new flake outputs that invalidate Nix's eval cache; with
the cache cold, Nix 2.30+ surfaces the dormant `flake.lock` /
`iohk-nix` blst v0.3.14↔v0.3.15 mismatch.

Bumps `inputs.iohkNix` from `fdfc53bc` (which expects blst
v0.3.14 in `overlays/crypto/libblst.nix`) to `9de00113` (which
expects v0.3.15, matching the v0.3.15 source the lock has been
resolving to since scaffold). `nix flake update iohkNix` refreshes
the transitive narHashes to the post-tarball-normalization
format. Same blst rev (`6d960cd05d6fe2b5bc9ba161edf0c1a131b87c4c`),
same closure semantics — only the metadata catches up.

`./llm/reviews/12/gate.sh` runs green: full `just CI`, including
unit tests, scenario schema, example-bake-determinism,
synthesis-report-shape, hlint, fourmolu, and compose acceptance
on `local-fast`.

## changes-requested

- commit message — title: `fix(nix): bump iohk-nix for blst v0.3.15 compatibility`
- commit message — body:
  > Updates the pinned `iohkNix` revision so the crypto overlay accepts the `blst` v0.3.15 source already resolved by the lock file. This keeps the dependency repair outside the seed-distribution task stack while unblocking fresh Nix evaluation for the new flake outputs tracked in #13.
