---
sha: 7b32aec0626e8adb58fdd35d811a9394dd3c5457
patch: wip-bump-iohk-nix-overlay-to-a
task: DEP-13
---

## ready

Round 2 — addresses the changes-requested verdict on
`680440486c08d8e32d4ef2c4d290559663aa3d2c.md`. Tree contents
unchanged; only the commit message was rewritten to the title +
body the reviewer dictated:

- Title: `fix(nix): bump iohk-nix for blst v0.3.15 compatibility`
- Body: explains that the iohkNix pin is moved past upstream's
  blst v0.3.15 fix so the crypto overlay's expected-version
  check matches the v0.3.15 source the lock file resolves; keeps
  the dep repair outside the seed-distribution task stack;
  references issue #13.

`./llm/reviews/12/gate.sh` re-run green: full `just CI`, including
unit tests, scenario schema, example-bake-determinism,
synthesis-report-shape, hlint, fourmolu, and compose acceptance
on `local-fast`.

## approved

The dependency bump is scoped to the `iohkNix` pin plus lock refresh, keeps the resolved `blst` source at v0.3.15, and now has the required commit title and rationale body.
