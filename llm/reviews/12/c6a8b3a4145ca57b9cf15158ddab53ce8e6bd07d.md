---
sha: c6a8b3a4145ca57b9cf15158ddab53ce8e6bd07d
patch: wip-t008-cosign-issue
task: T008
---

## ready

T008 from `specs/003-seed-distribution/tasks.md` —
cosign follow-up issue + FR-013 link. Lands on top of
T007 (`54f2564`).

### What

GitHub action (no commit):

- Filed
  [issue #14: `feat(distribution): sign seed images with cosign keyless`](https://github.com/lambdasistemi/cardano-testnet-baker/issues/14)
  in `lambdasistemi/cardano-testnet-baker`. Body links
  spec FR-013, names constitution §III pinning principle
  evidence (deterministic content-derived tags +
  offline-reproduction verification today; signatures
  tighten the trust model so consumers can verify without
  rebuilding), references Sigstore Fulcio + Rekor +
  GitHub Actions OIDC docs, and sketches the scope
  (`cosign sign --yes` step in the publish job, doc-side
  `cosign verify` flow, FR-013 follow-on update).
- Added the issue to the Planner board (paolino's project
  2) with `Category=Antithesis`, `Ownership=Work`, no
  `Status` set (per the task acceptance: belongs in the
  backlog, not in WIP yet).

Doc-only commit (this patch):

- `specs/003-seed-distribution/spec.md` — FR-013 is
  rewritten from "A follow-up issue MUST be filed" to
  link the now-filed issue directly. The constitution
  reference is preserved verbatim.

### Validates / acceptance

- T008 acceptance: "issue exists; spec FR-013 in this
  repo updated to link the new issue (separate doc-only
  commit)". The issue is at #14; FR-013 now links it.
- `gh issue view 14 --repo lambdasistemi/cardano-testnet-baker`
  returns the issue with the body described above.
- `gh project item-list 2 --owner paolino` lists the
  issue (verified via the GraphQL mutation that placed
  it on the board with Category and Ownership set).

### Notes for the reviewer

- Per pr-author skill rules, T009 (PR metadata: flip
  draft → ready, refresh body) is the reviewer's
  responsibility. I'm leaving the PR description and
  draft state untouched. The remaining reviewer-owned
  finalisation lives in the eventual
  `WaitingForFinalization` state once T009 is the only
  unfinished task.

### Local validation

Same caveat as earlier rounds: I cannot run
`./llm/reviews/12/gate.sh` locally because of the
CI-vs-local narHash divergence. CI re-runs on this push;
the change is spec text only and shouldn't disturb any
check.

## approved

- Issue #14 exists with the requested cosign keyless signing scope, and
  FR-013 now links that issue directly.
- The Planner item is present with `Category=Antithesis`, `Ownership=Work`,
  and no status set, matching T008's acceptance.
- The commit remains one vertical T008 docs slice and the reviewer-owned
  commit message gate passed.
