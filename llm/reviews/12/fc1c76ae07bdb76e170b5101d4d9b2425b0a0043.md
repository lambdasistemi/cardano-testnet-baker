---
sha: fc1c76ae07bdb76e170b5101d4d9b2425b0a0043
patch: wip-t007-docs
task: T007
---

## ready

T007 from `specs/003-seed-distribution/tasks.md` —
`docs/seed-distribution.md` plus README refresh. Lands on
top of T006 (`21ee706`).

### What

`docs/seed-distribution.md` (new):

- **Header:** what the feature publishes, where it lands,
  and the spec home for design rationale.
- **Tag scheme:** the primary/secondary tag grammar plus
  the explicit forbidden-tag list, cross-linked to
  [`contracts/artifact-identifier-scheme.md`](../specs/003-seed-distribution/contracts/artifact-identifier-scheme.md).
- **Image layout:** points at
  [`contracts/seed-image-layout.md`](../specs/003-seed-distribution/contracts/seed-image-layout.md)
  for the canonical `/seed/` tree without duplicating it.
- **§A Maintainer flow:** local build,
  `just publish-seed-images --dry-run`, optional
  archive-mode acceptance smoke test. Includes the exact
  four-line dry-run output the publish app emits per
  T005's contract.
- **§B Reviewer flow:** the offline reproduction
  walkthrough — clone at `<commit-sha-short>`, build
  the image locally, compare local vs registry manifest
  digest. Closes US3 by chaining T002's determinism
  oracle with T006's CI publish into a procedure the
  reviewer can run.
- **§C Consumer Dockerfile:** the
  `COPY --from=${SEED_REF}` snippet, plus the three
  pinning forms (primary, secondary, manifest-digest).
  Cross-linked to
  [`contracts/consumer-copy-from.md`](../specs/003-seed-distribution/contracts/consumer-copy-from.md).
- **CI surfaces:** brief description of the three jobs
  the feature touches (Build Gate determinism check,
  Compose acceptance, Publish seed images).
- **FAQ:** the same four questions
  `specs/003-seed-distribution/quickstart.md` answered,
  promoted to the consumer-facing doc tree.

`README.md`:

- **Status section** rewritten to call Features 001, 002,
  and 003 landed. Each feature is one paragraph naming
  what shipped and where the spec / docs live; Feature
  003's paragraph links `docs/seed-distribution.md` and
  the umbrella PR (#12). The previous "Feature 002 is
  preparing ChainDB seed synthesis" framing is gone.
- **New "Consuming the seed image" section** between the
  existing "Compose Acceptance" and "Development"
  sections. Carries the minimal copy-paste Dockerfile
  snippet for downstream operators and points at the
  full doc.

### Validates / acceptance

- Spec FR-010 ("a downstream operator can find, in this
  repo, exactly the copy-paste snippet they need to
  consume a seed"): the README's Consuming section gives
  the snippet inline; the deeper doc is one click away.
- Spec FR-011 (consumer-facing tag scheme is
  human-readable in repo docs): tag grammar is documented
  in both the new doc and the linked contract.
- SC-001 (downstream operator switches by changing one
  identifier): the README snippet uses
  `ARG SEED_REF=...`, which is the one identifier they
  flip.
- SC-006 (US1 independent test passes): a reviewer
  reading only `README.md` plus
  `docs/seed-distribution.md` can write a working
  consumer Dockerfile without consulting the spec or the
  contracts.
- All cross-references inside the new doc resolve
  (linked from `docs/` → `../specs/003-seed-distribution/`),
  matching the working tree layout.

### Local validation

Same caveat as earlier rounds: I cannot run the project's
`./llm/reviews/12/gate.sh` locally because of the
CI-vs-local narHash divergence noted in earlier rounds.
The `Build Gate` job is unaffected by this docs-only
change (no Nix derivation touched), but CI re-runs anyway.

### Notes for the reviewer

- Feature 003's PR link in the Status section is to
  PR #12 (this PR). Once merged, the link still works —
  GitHub keeps the URL alive for closed PRs. If you'd
  like a different post-merge target (e.g., the `main`
  commit SHA), say so and I'll update the README in the
  next round.
- I left the existing per-section structure of the README
  intact (Scenario Validation, Baking, ChainDB Seed
  Synthesis, Determinism, Compose Acceptance,
  Development, License). The new section slots between
  Compose Acceptance and Development because it is the
  *output* a developer pursues after acceptance has
  validated the artifact.

## changes-requested

- `README.md:125`: the consumer-facing text says the downstream
  Dockerfile uses "two `COPY --from=` lines", but the contract and the
  snippet immediately below copy four stable directories:
  `chain-db`, `genesis`, `pools`, and `utxo-keys`. This is not a
  formatting issue; it makes the README contradict the copy-paste
  integration surface T007 is supposed to make reliable. Please update
  the sentence to describe the seed payload copy accurately.

Reviewer-owned commit message gate passed; the blocker is the README's
consumer-facing correctness.
