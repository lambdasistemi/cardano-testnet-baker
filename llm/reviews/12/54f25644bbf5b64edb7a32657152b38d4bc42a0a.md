---
sha: 54f25644bbf5b64edb7a32657152b38d4bc42a0a
patch: wip-t007-docs
task: T007
---

## ready

Round 2 of T007 — addresses the round-1 verdict on
`fc1c76a`.

The reviewer correctly flagged that the README's
consumer section said the downstream Dockerfile uses
"two `COPY --from=` lines" while the snippet
immediately below has four. With T007 explicitly aimed at
making the consumer copy-paste path reliable, that
mismatch is exactly the kind of bug that lands in the
field as a half-broken Dockerfile.

### What changed

`README.md` "Consuming the seed image" lead-in is
rewritten to:

> A downstream Dockerfile pulls a seed with four
> `COPY --from=` lines — one per stable top-level
> directory under `/seed/` (`chain-db`, `genesis`,
> `pools`, `utxo-keys`):

That matches the snippet's four `COPY --from=seed`
lines verbatim and matches
[`contracts/consumer-copy-from.md`](../specs/003-seed-distribution/contracts/consumer-copy-from.md)'s
list of stable directories. `metadata.json` and
`synthesis-report.json` are intentionally still not in
the snippet (they're informational, not consumption
inputs); their position in the layout is documented in
`docs/seed-distribution.md`.

`docs/seed-distribution.md` is unchanged in this round —
it always said "two stable copy lines" only in the
maintainer-flow section about pulling tags, not in the
consumer-flow section, and the consumer Dockerfile there
already shows the four-line form.

### Validation

- README's "Consuming the seed image" intro now matches
  the four-line snippet beneath it.
- Contracts and `docs/seed-distribution.md` are
  unchanged.
- Same shellcheck / Build Gate impact as round 1: zero
  (docs-only change).

### Local validation

Same caveat as earlier rounds: I cannot run
`./llm/reviews/12/gate.sh` locally because of the
CI-vs-local narHash divergence. CI re-runs on this
push; the change is README text only and shouldn't
disturb any check.

## approved

- The README now describes the consumer Dockerfile accurately: four
  `COPY --from=` lines, one for each stable top-level seed directory
  (`chain-db`, `genesis`, `pools`, `utxo-keys`), matching both the
  snippet and `contracts/consumer-copy-from.md`.
- The new doc's linked contract targets are present, and the README keeps
  the consumer snippet one click from the deeper maintainer/reviewer flows.
- The commit remains one vertical T007 docs slice and the reviewer-owned
  commit message gate passed.
