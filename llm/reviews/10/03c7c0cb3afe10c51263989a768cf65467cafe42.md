---
sha: 03c7c0cb3afe10c51263989a768cf65467cafe42
patch: wip-t002-update-nix
task: T002 Update `nix/project.nix` source filtering to include `nix/iog-tools.nix` and synthesis docs where needed
previous-sha: 783e0876c2f44023e2cc9516f61771e27ad8554a
---

## ready

## changes-requested

- commit message body — title is good but the commit is title-only. Add a description (1–3 sentences) explaining *why* the filter is widening: `nix/[^/]+\.nix$` is needed so `nix/iog-tools.nix` (T001) becomes part of the cabal source so haskell.nix re-evaluates when top-level nix modules change; `specs/` is included so future spec/doc consumers can read from `specs/` without further filter churn. Run `stg edit -m "build(nix): widen cabal source filter to nix/ and specs/" -m "<body>"`.

Diff content unchanged from `783e0876c…` — semantics already validated in that round (filter additions correct, nixfmt reflow trusted by the gate).
