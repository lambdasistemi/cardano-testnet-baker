---
sha: 24e60ec2b8cc2637ac9a06e750318bd044a4cfa3
patch: wip-t003-update-flake.nix-and
task: T003 Update `flake.nix` and `nix/shell.nix` so the development shell and `nix run` wrapper can run `db-synthesizer`
---

## ready

## changes-requested

- commit message - title: `build(nix): expose db-synthesizer in flake outputs and dev shell`
- commit message - body:
  > Import the `iog-tools` module added in earlier patches and wire its
  > `db-synthesizer` derivation into `packages.db-synthesizer` and
  > `apps.db-synthesizer`, so `nix build .#db-synthesizer` and `nix run
  > .#db-synthesizer` resolve. Pass it through to `nix/shell.nix` and
  > append to `buildInputs` so the executable is on `PATH` inside `nix
  > develop`. Other hunks are `nixfmt-classic` reflow on existing
  > declarations.

  Run `stg edit -m "build(nix): expose db-synthesizer in flake outputs and dev shell" -m "<body>"` (repeated `-m` becomes separate paragraphs).

Semantics: `packages.db-synthesizer = iogTools.db-synthesizer` and `apps.db-synthesizer.program = "${iogTools.db-synthesizer}/bin/db-synthesizer"` are independent of the colon-namespaced `<pkg>:exe:<exe>` keys produced by `flakePkgs.packages`, so no overlap. Dev-shell uses `++ [ iogTools.db-synthesizer ]` outside the `with pkgs;` scope - correct since `iogTools` is not in `pkgs`. No naming conflicts, defaults preserved.
