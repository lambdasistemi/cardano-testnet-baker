---
sha: 56f95fb1ea382cac5c3fb01b208a7ee900f061bc
patch: synthesis-real-runner
task: T020/T021/T022/T023/T024/T025
previous-sha: eb6f3083979ce08d4193cf515e551514375e6cc1
rolled-back-from: 161ddccc0fb3afdf6e66a60fd7d472a2083d4b9a
---

## ready (rollback)

Addressed review findings:
- Added direct Shelley genesis coverage for pool registrations,
  pool-to-stake delegation, pool VRF hash, and pool stake initial funds.
- Documented the intentional reuse of `poolStakeKeyLabel` across the
  `PoolStakeKey` and `PoolStakePaymentKey` roles; the derivation role tag
  separates the derived keys.
- Applied the reviewer-specified commit title and body.

Evidence:
- Focused Nix unit run passed for the new pool-stake registration test and
  runner args: 3 examples, 0 failures.
- Previous scratch `local-fast` bake with this rollback family produced a
  non-empty ChainDB: `du -sb tmp/rollback-local-fast/chain-db` reported
  69944 bytes, with non-empty immutable chunks and ledger state.

Gate:
- `./llm/reviews/10/gate.sh` exited 0 after action runtime checks,
  shellcheck, Build Gate, formatting, HLint, scenario validation, 72 unit
  examples, synthesized `local-fast`, compose acceptance, and
  `git diff --check`.

## approved

Ready file now present with `previous-sha` and `rolled-back-from`. Title and body match. The `Keys.hs:189` comment records why `poolStakeKeyLabel` is intentionally reused across `PoolStakeKey` and `PoolStakePaymentKey`. The new BakeSpec test (`registers pool stake in Shelley genesis`) decodes the rendered shelley-genesis.json and asserts pool registrations (`publicKey`, `vrf`), `staking.stake` delegations, and pool-stake `initialFunds` lovelace match the deterministic derivations — exactly the coverage the prior round called for. The author's local 69944-byte ChainDB observation backs the rollback's premise. Subsequent T026–T035 replays will need fresh measurement evidence.
