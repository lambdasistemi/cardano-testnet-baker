---
sha: 49ff667bbc47b0a4b5baa1dfe8e5781b72d1697c
patch: synthesis-cli-validation
task: T016/T021
---

## ready

Red evidence:
- `nix run .#unit-tests --quiet -- --match "reports synthesis validation failures"` failed before implementation with a `PatternMatchFail` in `Cardano.Testnet.Baker.CLI.showValidationFailure`.

Scope note:
- This combines T016 with the minimal T021 diagnostic implementation needed to keep the author gate green.

Gate:
- `./llm/reviews/10/gate.sh` exited 0 after 67 unit examples, scenario validation, HLint/format checks, and compose acceptance for `local-fast` and `normal`.

## changes-requested

- commit message — title: `feat(cli): render synthesis validation failures as user-visible diagnostics`
- commit message — body:
  > Extends `showValidationFailure` to render the three synthesis
  > validation constructors added in the prior validation patch:
  > `SynthesisSlotCountRequired`, `SynthesisSlotCountNotPositive`, and
  > `SynthesisProfileEmpty`. Combines T016 (red diagnostic test) with
  > the minimal T021 implementation because T016 alone fails the gate:
  > a partial pattern in `showValidationFailure` would raise
  > `PatternMatchFail` on any synthesis-validation error.

  Run `stg edit -m "feat(cli): render synthesis validation failures as user-visible diagnostics" -m "<body>"`.

Findings:

- `test/Cardano/Testnet/Baker/CLISpec.hs` — only `SynthesisSlotCountRequired`
  is exercised. The other two diagnostics ship without coverage. Add:
  - A test asserting `SynthesisSlotCountNotPositive 0` renders with
    the offending value embedded (e.g. `isInfixOf "got 0"`) so a
    future regression dropping `show slotCount` is caught.
  - An equivalent for `SynthesisProfileEmpty` — either via a CLI
    fixture with `synthesis.profile = ""` or a direct
    `showValidationFailure` assertion.
  Direct unit tests on `showValidationFailure` are cheaper than a
  full CLI invocation and sufficient to pin the rendering contract.
  Without these, two of the three new branches have no test
  coverage at all.
