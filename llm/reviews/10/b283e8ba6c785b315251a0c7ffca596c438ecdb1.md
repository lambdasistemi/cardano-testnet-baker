---
sha: b283e8ba6c785b315251a0c7ffca596c438ecdb1
patch: scenario-normal-synthesis
task: T011 Update `examples/scenarios/normal.json` with a realistic measurement synthesis request
---

## ready

## changes-requested

- commit message — title: `feat(scenarios): enable realistic-measurement synthesis on normal example`
- commit message — body:
  > Adds a synthesis block (`enabled: true`, `slotCount: 300000`,
  > `profile: "normal-realistic"`) to `examples/scenarios/normal.json`
  > as the measurement-realistic counterpart to the CI-fast
  > `local-fast` example. The two committed scenarios now exercise
  > both ends of the synthesis size spectrum — one tuned for
  > quick-feedback CI runs and one tuned for representative
  > measurement.

  Run `stg edit -m "feat(scenarios): enable realistic-measurement synthesis on normal example" -m "<body>"`.

Semantics: 300000 slots is large but within `slotCount: { minimum: 1 }`; profile name distinguishes the realistic scenario from the CI-fast one. Implicitly covered by `loadScenario "examples/scenarios/normal.json"` in `ScenarioSpec`. Note: the prior patch's body mentioned that `normal.json` was left without synthesis as a coverage choice — that framing was provisional; this patch supersedes it.
