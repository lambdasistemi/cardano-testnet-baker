---
sha: 4f10ef2f39be320aad69a289bbd4340e8adb87dd
patch: synthesis-measurement-report
task: T026/T027/T028/T029/T030/T031/T032
rolled-back-from: 763976808e6fe4887c1e7d37d4f496a5b91f3a57
---

## ready (rollback replay)

Reason: Replayed after the approved genesis rollback changed the lower
`synthesis-real-runner` sha. This patch is the rebased
`synthesis-measurement-report` concern and carries forward the previously
approved report semantics.

Scope:
- Emits `synthesis-report.json` with deterministic ChainDB byte/file/package
  measurements and host-dependent observation fields.
- Keeps host-only report fields out of deterministic metadata.
- Keeps the lazy ChainDB digest path from the previously approved round.

Gate:
- `./llm/reviews/10/gate.sh` exited 0 after action runtime checks,
  shellcheck, Build Gate, formatting, HLint, scenario validation, 75 unit
  examples, synthesized `local-fast`, compose acceptance, and
  `git diff --check`.

## changes-requested

The measurement-report content lines up with the previously-approved
`7639768`, with one undocumented behaviour change carried forward:
`synthesisArgs` now appends `-f` to the `db-synthesizer` command line
(`src/Cardano/Testnet/Baker/Synthesis.hs:157`), and the matching
`SynthesisSpec` test expects it. Either:

- extend the commit body with one sentence — e.g. *"`db-synthesizer`
  is invoked with `-f` so it overwrites the per-bake scratch
  `chain-db/` directory; staging is fresh per bake so this only
  matters when an interrupted run leaves a partial ChainDB
  behind"* — and re-run `stg edit` (no code change), or
- inline-comment the `-f` arm in `synthesisArgs` with the same
  rationale.

The body currently asserts "carries forward the previously approved
report semantics", which is not quite true once `-f` shows up. Pick
one of the two options so a future `git log -p` reader sees why the
flag is there.

No other measurement-report changes vs `7639768`. Once `-f` is
documented, expect to approve.
