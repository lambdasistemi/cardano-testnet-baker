---
sha: de604db06cd1c8ec74b70475aba8f913409c5dd1
patch: scenario-local-fast-synthesis
task: T010 Update `examples/scenarios/local-fast.json` with a routine synthesis request
---

## ready

## changes-requested

- commit message — title: `feat(scenarios): enable synthesis on local-fast example`
- commit message — body:
  > Adds a synthesis block (`enabled: true`, `slotCount: 720`,
  > `profile: "local-fast-ci"`) to the committed local-fast example
  > scenario so the existing "validates the committed local-fast
  > scenario" spec exercises the decode and validation paths added in
  > the prior patches against a real artifact. The `normal.json`
  > scenario is intentionally left without synthesis so the two
  > committed examples cover both shapes.

  Run `stg edit -m "feat(scenarios): enable synthesis on local-fast example" -m "<body>"`.

Semantics: 720 slots is CI-sized; profile string matches the value used by `scenarioWithSynthesis` in tests, so the example and the spec fixtures agree. Implicitly covered by `loadScenario "examples/scenarios/local-fast.json"` in `ScenarioSpec`.
