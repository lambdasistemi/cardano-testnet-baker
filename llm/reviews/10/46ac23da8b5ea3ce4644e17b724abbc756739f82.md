---
sha: 46ac23da8b5ea3ce4644e17b724abbc756739f82
patch: synthesis-bulk-credentials
task: T014/T017
---

## ready

Red evidence:
- `nix run .#unit-tests --quiet -- --match "synthesis bulk credentials"` failed before implementation with `Could not find module 'Cardano.Testnet.Baker.Synthesis'`.

Scope note:
- This combines T014 with the minimal T017 implementation because T014 is a red-test task and the author gate must be green before handoff.

Gate:
- `./llm/reviews/10/gate.sh` exited 0 after 61 unit examples, scenario validation, and compose acceptance for `local-fast` and `normal`.

## changes-requested

- commit message — title: `feat(synthesis): render db-synthesizer bulk credentials from pool artifacts`
- commit message — body:
  > Adds `Cardano.Testnet.Baker.Synthesis` with `BulkCredential`,
  > `bulkCredentialFromPoolArtifacts`, and `renderBulkCredentials`,
  > plus a `SynthesisError` ADT for envelope-decode failures. The
  > module re-decodes each text envelope via `eitherDecode` so the
  > inner JSON values flow into the canonical
  > `[[opcert, vrf, kes], ...]` array shape consumed by upstream
  > `db-synthesizer`. Combines T014 (red contract test) with T017
  > (minimal implementation) because T014 alone fails the green-gate
  > handoff.

  Run `stg edit -m "feat(synthesis): render db-synthesizer bulk credentials from pool artifacts" -m "<body>"`.

Findings:

- `test/Cardano/Testnet/Baker/SynthesisSpec.hs` — single positive
  test only. Add:
  - `renderBulkCredentials []` returns `Right "[]"` (canonical empty
    JSON array). One-line test that pins the boundary.
  - A garbage-envelope `BulkCredential` returns `Left
    (SynthesisInvalidTextEnvelope "operationalCertificate" _)`. Without
    this, the `parseEnvelope` error path is dead code from the gate's
    point of view.
- `src/Cardano/Testnet/Baker/Synthesis.hs:69` — `bulkCredentialTuple`
  returns `[Value]`, not a tuple. Rename to `bulkCredentialEnvelopes`
  (or similar) so the type and name agree; the three-element shape is
  enforced by call-site contract, not Haskell's tuple type.
- `src/Cardano/Testnet/Baker/Synthesis.hs:14` — `BulkCredential (..)`
  exports the data constructor. If `bulkCredentialFromPoolArtifacts`
  is the only intended construction path, hide the constructor and
  expose accessors instead so callers cannot bypass the safe
  derivation. Non-blocking — the test already goes through the safe
  constructor — but worth tightening before the module accumulates
  more callers.
