---
sha: 783e0876c2f44023e2cc9516f61771e27ad8554a
patch: wip-t002-update-nix
task: T002 Update `nix/project.nix` source filtering to include `nix/iog-tools.nix` and synthesis docs where needed
---

## ready

## changes-requested

- commit title — rename from `wip: T002 Update nix/project.nix source filtering to include nix/iog-tools.nix and synthesis docs where needed` to `build(nix): widen cabal source filter to nix/ and specs/`. Drop `wip:`, `T002`, and the file path. Run `stg edit -m "build(nix): widen cabal source filter to nix/ and specs/"`.

Semantics: two new clauses on `cleanSourceWith.filter` — `.*/nix/[^/]+\.nix$` and `.*/specs(/.*)?$`. The `nix/` clause is bounded to immediate children (no nested matches), so haskell.nix only re-evaluates when top-level nix modules change. `specs/` widening is forward-looking; no current consumer reads from there but the source-hash dependency is harmless. Remainder of the diff is `nixfmt` reflow on `fix-libs`, `inputMap`, and `buildInputs` — mechanical, trusted by the gate.
