---
sha: 02bcf8886fe3e92b229f20f87109e64f88b282ec
patch: fixture-genesis-only-coverage
task: T012 Update `test/data/minimal-scenario.json` or fixtures so genesis-only scenarios remain covered
---

## ready

## changes-requested

- commit message — title: `test(scenario): pin minimal fixture as genesis-only coverage`
- commit message — body:
  > After T010 and T011 the two committed example scenarios both carry
  > synthesis blocks, so the no-synthesis shape was no longer covered
  > at the scenario layer. Adds a unit test that loads
  > `test/data/minimal-scenario.json`, asserts
  > `scenarioSynthesis == Nothing`, and validates the result. The
  > fixture itself is unchanged; this patch only locks it in as the
  > genesis-only smoke case alongside its existing use in
  > `BakeSpec.hs`.

  Run `stg edit -m "test(scenario): pin minimal fixture as genesis-only coverage" -m "<body>"`.

Semantics: targeted single-test addition, no production-code changes. The `scenarioSynthesis == Nothing` assertion guards against accidental addition of synthesis to the minimal fixture and against regressions in the optional-decoder path.
