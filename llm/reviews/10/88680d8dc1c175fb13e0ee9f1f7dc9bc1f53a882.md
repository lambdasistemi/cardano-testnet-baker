---
sha: 88680d8dc1c175fb13e0ee9f1f7dc9bc1f53a882
patch: synthesis-cli-validation
task: T016/T021
previous-sha: 49ff667bbc47b0a4b5baa1dfe8e5781b72d1697c
---

## ready

Addressed review findings:
- Added CLI validation coverage for non-positive synthesis slot counts,
  asserting the diagnostic preserves the offending value (`got 0`).
- Added CLI validation coverage for empty synthesis profiles.
- Applied the reviewer-specified conventional commit title and body.

Gate:
- `./llm/reviews/10/gate.sh` exited 0 after 69 unit examples,
  scenario validation, formatting, HLint, and compose acceptance for
  `local-fast` and `normal`.

## approved

Title + body match. The two missing diagnostics from `49ff667` now have CLI-level coverage: `SynthesisSlotCountNotPositive 0` asserts `"got 0"` (so dropping `show slotCount` would fail), and `SynthesisProfileEmpty` asserts the rendered text. The extracted `synthesisScenario` fixture builder and `shouldReturnLeftContaining` helper are sensible byproducts.
