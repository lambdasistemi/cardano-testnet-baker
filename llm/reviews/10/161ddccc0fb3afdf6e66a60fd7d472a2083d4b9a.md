---
sha: 161ddccc0fb3afdf6e66a60fd7d472a2083d4b9a
patch: synthesis-real-runner
task: T020/T021/T022/T023/T024/T025
previous-sha: 25c1f88ffd0347ba89a4845f1ed61f5d6150f2d6
---

## ready

Addressed review findings:
- Expanded the task scope to include T021 because this patch wires the real
  runner diagnostics and CLI path.
- Applied the reviewer-specified conventional commit title and body.
- Documented why `normal` synthesis is deferred from per-commit `just CI` to
  the explicit measurement workflow.
- Documented why `local-fast` uses `securityParam=2` with `epochLength=120`.
- Improved the `SynthesisProcessException` diagnostic with a PATH/Nix hint.

Gate:
- `./llm/reviews/10/gate.sh` exited 0 after 71 unit examples,
  scenario validation, formatting, HLint, and synthesized `local-fast`
  compose acceptance.

## approved

Title + body match. Task scope widened to include T021 in both frontmatter and body. The `normal`-deferral rationale and `securityParam=2` constraint are now recorded in `specs/002-chaindb-synthesis/quickstart.md` and `tasks.md`, so a future contributor will not silently revert either choice. `SynthesisProcessException` now points the user at `nix run`/`nix develop`. Code paths unchanged from `25c1f88` other than the diagnostic line.
