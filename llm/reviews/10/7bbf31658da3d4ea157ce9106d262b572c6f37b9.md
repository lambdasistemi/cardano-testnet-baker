---
sha: 7bbf31658da3d4ea157ce9106d262b572c6f37b9
patch: synthesis-bake-orchestration
task: T015/T018/T019
---

## ready

Red evidence:
- `nix run .#unit-tests --quiet -- --match "stages synthesized ChainDB"` failed before implementation because `Cardano.Testnet.Baker.Bake` did not export `bakeScenarioWithSynthesisRunner` and `Cardano.Testnet.Baker.Synthesis` did not export `SynthesisRun`/`SynthesisRunner`.

Scope note:
- This combines T015 with the minimal T018/T019 runner and bake orchestration needed to keep the author gate green.
- The injected runner path stages `chain-db/` for synthesis-enabled scenarios and skips synthesis for genesis-only scenarios. The default CLI bake path remains unchanged until the later CLI/real-runner task.

Gate:
- `./llm/reviews/10/gate.sh` exited 0 after 65 unit examples, scenario validation, HLint/format checks, and compose acceptance for `local-fast` and `normal`.

## changes-requested

- commit message — title: `feat(bake): orchestrate ChainDB synthesis through an injected runner`
- commit message — body:
  > Adds `SynthesisRunner` (a `SynthesisRun -> IO (Either SynthesisError ())`
  > effect boundary) and a new `bakeScenarioWithSynthesisRunner` entry
  > point that, when the scenario has synthesis enabled, renders bulk
  > credentials, invokes the runner with the staged paths, and keeps
  > the resulting `chain-db/` alongside the rest of the bake output.
  > Genesis-only scenarios bypass the runner entirely. `bakeScenario`
  > continues to mean "bake without synthesis" until the CLI wires the
  > real upstream runner in a follow-up task. Combines T015 (red
  > integration tests), T018 (runner type + bake plumbing), and T019
  > (genesis-only skip) because T015 alone fails the green gate.

  Run `stg edit -m "feat(bake): orchestrate ChainDB synthesis through an injected runner" -m "<body>"`.

Findings:

- `test/Cardano/Testnet/Baker/BakeSpec.hs` — no test for the
  runner-returns-Left path. Add a third case where the runner returns
  `Left (SynthesisInvalidTextEnvelope ...)` and assert (a) the bake
  returns `Left (BakeSynthesisFailed _)`, and (b) `outputDir` was not
  produced (the staged `chain-db/` and `.synthesis/` are cleaned up by
  the outer `writeThenRename` `onException` path, but that branch is
  currently unverified). Without this, the new error constructor and
  the cleanup it relies on are untested.
- `src/Cardano/Testnet/Baker/Bake.hs:122` — `bakeScenario` is now a
  silent no-synthesis variant: a caller passing a synthesis-enabled
  scenario through `bakeScenario` gets a bake without `chain-db/` and
  no error. The CLI temporarily relies on this, per the scope note,
  but the function name no longer matches its semantics. Either:
  - rename to `bakeScenarioWithoutSynthesis`, or
  - delete the public wrapper and require every call site to pass an
    explicit runner (callers that genuinely don't want synthesis pass
    a `noopRunner` whose `runSynthesis _ = pure (Right ())`).
  Either change makes the "did this bake run synthesis?" question
  visible at every call site. Pick one and apply it before the CLI
  wiring task lands.
