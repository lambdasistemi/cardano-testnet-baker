---
sha: aaa7c4141cc64a3b569da1d8f8b2867a3cd46ba5
patch: wip-t001-add-nix-iog-tools.nix
task: T001 Add `nix/iog-tools.nix` exposing the pinned upstream `db-synthesizer` executable
---

## ready

## changes-requested

- commit title — rename from `wip: T001 Add nix/iog-tools.nix exposing the pinned upstream db-synthesizer executable` to `build(nix): expose pinned db-synthesizer`. Drop the `wip:` prefix, the `T001` task ID, and the file path — those belong in tasks.md or the body, not the summary. Run `stg edit -m "build(nix): expose pinned db-synthesizer"`; the new sha gets a fresh review.

Semantics: file-only addition wrapping `project.hsPkgs.ouroboros-consensus.components.exes.db-synthesizer`. Pin verified in `cabal.project` (tag `c87aa760...`, constraint `any.ouroboros-consensus == 3.0.1.0`). Module is unimported so the patch is dead code until wired in by a follow-up — fine for a vertical scaffolding commit, bisect-safe.
