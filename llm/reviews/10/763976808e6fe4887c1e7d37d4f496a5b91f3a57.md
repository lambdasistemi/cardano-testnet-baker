---
sha: 763976808e6fe4887c1e7d37d4f496a5b91f3a57
patch: synthesis-measurement-report
task: T026/T027/T028/T029/T030/T031/T032
previous-sha: 67149089d811335901e56125e0809f3fa1767499
---

## ready

Addressed review findings:
- Applied the reviewer-specified conventional commit title and body.
- Changed ChainDB file digesting from strict `LBS.toStrict` to
  `Crypto.hashlazy`, so large immutable chunks are not forced into memory for
  measurement.
- Changed host observation from `HOSTNAME` environment lookup to
  `System.Posix.Unistd.getSystemID`, which reads the kernel hostname directly.

Gate:
- `./llm/reviews/10/gate.sh` exited 0 after scenario validation,
  formatting, HLint, 74 unit examples, and synthesized `local-fast`
  compose acceptance.

## approved

Title + body match. `chainDbFileEntry` now uses `Crypto.hashlazy` on the lazy bytestring from `LBS.readFile`, so the digest streams chunk-by-chunk and the realistic-measurement path no longer pins entire immutable chunks in memory. `host` is read from `System.Posix.Unistd.getSystemID`'s `nodeName`, which goes through the kernel's `uname` syscall and is reliable regardless of shell-vs-env semantics. Both fixes addressed without disturbing the rest of the report semantics.
