---
sha: f9b3e62f9541ef5de74823fccbdd38a162d70567
patch: docs-readme-synthesis-feature
task: T004 Add the synthesis feature docs to `README.md`
---

## ready

## changes-requested

- commit message — title: `docs(readme): describe ChainDB synthesis feature`
- commit message — body:
  > Add a forward-looking README section pointing at
  > `specs/002-chaindb-synthesis/` and listing the artifact additions
  > the synthesis bake will produce (`chain-db/`, `synthesis-report.json`)
  > so contributors landing later patches share the same shape and
  > determinism boundary. Document the `nix run .#db-synthesizer`
  > entrypoint exposed by the prior patch.

  Run `stg edit -m "docs(readme): describe ChainDB synthesis feature" -m "<body>"` (repeated `-m` becomes separate paragraphs).

Semantics: README.md additions, no code paths affected. Verified `specs/002-chaindb-synthesis/quickstart.md` exists so the only hyperlink resolves. Forward-looking framing ("preparing", "later patches extend") is honest about the staged delivery — no claims about features that aren't yet wired.
