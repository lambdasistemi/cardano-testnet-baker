---
sha: 06bc1371b86c0fc8ee6e06d1c445497da12a494d
patch: synthesis-measure-normal
task: T033/T034/T035
previous-sha: e5def67d292f8fff895ac6499aeb08df71418d80
rolled-back-from: e5def67d292f8fff895ac6499aeb08df71418d80
---

## ready (rollback replay)

Addressed review findings:
- Re-collected the `normal` 300k-slot measurement after the approved genesis
  rollback made synthesis forge real blocks.
- Updated quickstart evidence from the previous degenerate
  `chainDb.bytes=5` output to the new measured report:
  `bytes=22381220`, `fileCount=217`, `packagedBytes=22408383`,
  `wallTimeMilliseconds=122351`.
- Tightened `synthesis-report-shape` so local-fast reports must have
  non-trivial ChainDB size (`bytes > 1024`, `fileCount >= 10`, and
  `packagedBytes > bytes`).
- Applied the reviewer-specified commit title and body.

Evidence:
- `just measure-normal tmp/synthesis/normal` exited 0 and printed the
  non-degenerate `normal` report above.
- `nix build --quiet .#checks.x86_64-linux.synthesis-report-shape` exited 0.

Gate:
- `./llm/reviews/10/gate.sh` exited 0 after action runtime checks,
  shellcheck, Build Gate, formatting, HLint, scenario validation, 75 unit
  examples, synthesized `local-fast`, compose acceptance, and
  `git diff --check`.

## approved

Realistic measurement evidence captured: 22.3 MB / 217 files / 22.4 MB packaged / ~122 s wall time, replacing the prior degenerate `bytes=5, fileCount=4` numbers. `synthesis-report-shape` bounds tightened to `bytes > 1024`, `fileCount >= 10`, and `packagedBytes > bytes`, so a future regression that returns to a manifest-only ChainDB fails the gate. Title + body match. Quickstart now records the post-rollback measurement.
